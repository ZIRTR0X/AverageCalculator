import XCTest
@testable import CalculatorAverageVM

final class CalculatorAverageVMTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CalculatorAverageVM().text, "Hello, World!")
    }
}
