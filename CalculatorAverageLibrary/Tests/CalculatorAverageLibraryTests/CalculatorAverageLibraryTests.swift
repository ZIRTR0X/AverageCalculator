import XCTest
@testable import CalculatorAverageLibrary

final class CalculatorAverageLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CalculatorAverageLibrary().text, "Hello, World!")
    }
}
